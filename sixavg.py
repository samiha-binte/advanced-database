# List of recorded execution times (only the "real" time values)
times = [0.001, 0.001, 0.000, 0.000, 0.001, 0.000, 0.001, 0.001, 0.030, 3.482]

# Calculate the average time
average_time = sum(times) / len(times)

# Print the result
print(f"Average Execution Time: {average_time:.6f} seconds")
